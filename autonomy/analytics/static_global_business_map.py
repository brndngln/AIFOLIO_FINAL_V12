"""
AIFOLIO SAFE AI Global Business Map
- Static, admin-reviewed, aggregate only
"""
def global_business_map(locations):
    # Expects: list of {'region': str, 'vaults': int}
    return {'global_business_map': locations}
