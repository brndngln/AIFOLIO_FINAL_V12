// AR Preview: AR-based product previews
export function showARPreview(product) {
  // Stub: Show AR preview for product
  return {
    productId: product.businessId,
    arEnabled: true,
    previewUrl: product.previewUrl,
  };
}
